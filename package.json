{
  "name": "nextjs-cdk-static-i18n-template",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "front",
    "cdk"
  ],
  "scripts": {
    "dev:front": "npm --prefix front run dev",
    "build:front": "npm --prefix front run build",
    "shadcn:add": "npm --prefix front run shadcn:add",
    "deploy:cdk": "npm --workspace cdk run deploy"
  }
}
