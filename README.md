# Next.js CDK Static i18n Template

Template repository for a static Next.js app deployed to AWS (S3 + CloudFront via CDK).  
Build the frontend, run `cdk deploy`, and you’ll get a multi-language static site served over CloudFront with a default locale redirect from `/`.